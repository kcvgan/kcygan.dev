---
import AshbyLink from '../components/AshbyLink.astro';
import PostList from '../components/PostList.astro';
import Layout from '../layouts/Layout.astro';
import { getPublishedPosts } from '../lib/posts';

const posts = (await getPublishedPosts()).slice(0, 5);
---

<Layout
  title="Home"
  description="Kacper Cygan is a product engineer at Ashby who publishes fiction, nonfiction, and writing about software and products."
>
  <div class="home">
    <section class="intro" aria-labelledby="intro-title">
      <h1 id="intro-title">I build products and write.</h1>
      <p class="lede">
        I’m a product engineer at <AshbyLink />. I like to write about different
        things—software, products, work, and whatever else catches my attention. I
        publish fiction and nonfiction here.
      </p>
      <p class="elsewhere">
        <a href="mailto:hello@kcygan.dev">Email me</a>
      </p>
    </section>

    <section class="writing" aria-labelledby="writing-title">
      <div class="section-heading">
        <h2 id="writing-title">Writing</h2>
        <a href="/blog">All <span aria-hidden="true">→</span></a>
      </div>
      {posts.length > 0 ? (
        <PostList posts={posts} />
      ) : (
        <p class="empty">The first piece is on its way.</p>
      )}
    </section>
  </div>
</Layout>

<style>
  .home {
    width: min(100% - 3rem, var(--measure));
    margin: 4.5rem auto 0;
  }

  .intro {
    max-width: 41rem;
  }

  h1 {
    max-width: 22ch;
    margin: 0;
    font-family: var(--font-serif);
    font-size: clamp(2rem, 4vw, 2.85rem);
    font-weight: 600;
    letter-spacing: -0.03em;
    line-height: 1.08;
    text-wrap: balance;
  }

  .lede {
    max-width: 37rem;
    margin: 1.25rem 0 0;
    color: #4d4a44;
    font-size: 1.08rem;
    line-height: 1.7;
    text-wrap: pretty;
  }

  .elsewhere {
    display: flex;
    gap: 0.7rem;
    align-items: center;
    margin: 1.25rem 0 0;
    color: var(--muted);
    font-size: 0.88rem;
  }

  .elsewhere a {
    color: var(--ink);
  }

  .writing {
    margin-top: 5rem;
  }

  .section-heading {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 1rem;
    margin-bottom: 1.1rem;
  }

  .section-heading h2 {
    margin: 0;
    font-family: var(--font-serif);
    font-size: 1.35rem;
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  .section-heading a {
    color: var(--muted);
    font-size: 0.78rem;
    text-decoration: none;
  }

  .section-heading a:hover {
    color: var(--accent);
  }

  .empty {
    padding-block: 1.25rem;
    border-block: 1px solid var(--rule);
    color: var(--muted);
  }

  @media (max-width: 36rem) {
    .home {
      width: min(100% - 2.5rem, var(--measure));
      margin-top: 3rem;
    }

    h1 {
      font-size: 2rem;
    }

    .lede {
      margin-top: 1.5rem;
      font-size: 1.02rem;
    }

    .writing {
      margin-top: 4rem;
    }
  }
</style>
